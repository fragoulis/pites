package autofix

// AddressCity takes in a legacy raw address string and returns the ID
// of a existing address city record or an empty string if a match could
// not be found.
func AddressCity(legacyAddress string) string {
	return legacyAddress
}
